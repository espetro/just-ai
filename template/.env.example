# Copy to .env (nitro dev) AND .dev.vars (wrangler preview). Both gitignored.
# Production secrets: wrangler secret put <NAME> --name <worker-name>

# Provider keys — a lane is skipped automatically when its key is absent
GROQ_API_KEY=
ZAI_API_KEY=
GOOGLE_AI_API_KEY=
# OPENROUTER_API_KEY=

TURNSTILE_SECRET_KEY=
